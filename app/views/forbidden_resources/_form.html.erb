<%= form_for(@forbidden_resource) do |f| %>
  <% if @forbidden_resource.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@forbidden_resource.errors.count, "error") %> prohibited this forbidden_resource from being saved:</h2>

      <ul>
      <% @forbidden_resource.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
